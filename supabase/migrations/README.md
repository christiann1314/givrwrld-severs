# Database Migrations

This directory is for Supabase database migrations.

## Status

All previous migrations have been archived to `../migrations-archive/` to allow for a fresh start with correct migrations.

## Next Steps

1. Create new migration files with correct schema
2. Run migrations in Supabase SQL Editor
3. Verify all tables are created correctly

## Migration Naming

Use format: `YYYYMMDDHHMMSS_description.sql`

Example: `20251109000000_initial_schema.sql`

